version: "2"

run:
  timeout: 5m

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - ineffassign
    - misspell
    - unconvert
    - nilerr
    - gocritic

formatters:
  enable:
    - goimports
    - gofmt
